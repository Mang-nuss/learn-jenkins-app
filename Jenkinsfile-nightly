pipeline {
    agent any
    
    stages {

        stage('build Docker image at night') {
            steps {
                sh 'docker build -f ci/Dockerfile -t my-playwright .' //build this in the current dir.
            }
        }
    }
}